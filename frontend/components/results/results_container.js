import { connect } from "react-redux";
import Results from "./results";

const mstp = (state) => {
  return {};
};

const mdtp = (dispatch) => {
  return {};
};

export default connect(mstp, mdtp)(Results);
